namespace Tenderfoot.Mvc
{
    public enum HttpMethod { GET, POST, PUT, DELETE }
    public enum InputType { All, Alphabet, AlphaNumeric, Email, Enum, FileName, FilePath, DateTime, Numeric, String, URL }
    public enum FileType { CSV, Image }
}
